import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'geo.alert.test',
  appName: 'geoAlert',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
